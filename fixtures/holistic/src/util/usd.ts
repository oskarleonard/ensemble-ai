// The USD family for MONEY decisions (fee waivers, deposit gating). Widening this set
// changes what the app treats as a dollar — a domain rule, never a presentation choice.
export const USD_FAMILY = ['usd', 'usdc', 'usdt'];
