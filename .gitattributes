# dist/ is generated by `npm run build` (tsup) but COMMITTED on purpose, so a
# `github:` git-dependency installs with no build step. Mark it generated so it
# collapses in diffs and doesn't skew language stats.
dist/** linguist-generated=true
dist/** -diff
